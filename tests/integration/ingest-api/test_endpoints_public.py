"""
Tests for public ingest-api endpoints accessible without authentication.

Auth classification source:
  gateway/api_endpoints.localhost.json, branch karlburke/CaptureAPIUsage

Run all public endpoint tests:
    TEST_API="ingest-api" pytest tests/localhost/integration/ingest-api/ -v
"""

import requests

TIMEOUT = 10

# TODO: implement tests
